# Copy to .env (dev) / .env.production (build) — Vite only exposes vars prefixed VITE_.
# Never hardcode this in source; every API call reads it from here.
VITE_API_BASE_URL=https://gymbackendorg.vercel.app
